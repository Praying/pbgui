/**
 * Legacy `confirmDialog(options)` → window.PBGuiDialogs.confirm bridge —
 * now a thin re-export of the shared module (src/shared/lib/dialogs.ts).
 */
export { dialogsConfirm } from '@/shared/lib/dialogs';
